DB_SERVER=scolarodb.database.windows.net
DB_DATABASE=BGOTY
DB_USER=
DB_PASSWORD=

# Google OAuth — only a Client ID is needed (no secret).
# Get one at https://console.cloud.google.com/apis/credentials
# Set NEXT_PUBLIC_GOOGLE_CLIENT_ID and the server code will use it too.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

JWT_SECRET=
